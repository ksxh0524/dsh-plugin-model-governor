# dsh-plugin-model-governor layer：模型治理面（server；浏览器半 S5 第二波另行挂载）。
# 空配置 = 自带行为：不限流 + opencode 系会话头默认开（与 dsh-opencode-session 默认一致）。
- insert:
    - id: model-governor
      name: dsh-plugin-model-governor
      config: {}
